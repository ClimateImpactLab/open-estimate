name: test_env
channels:
  - conda-forge
dependencies:
  - python=3.7
  - numpy
  - scipy
  - emcee
  - statsmodels
  - xarray
  - pytest
  - pytest-mock
  - coverage
  - pip
  - pip:
      - metacsv
